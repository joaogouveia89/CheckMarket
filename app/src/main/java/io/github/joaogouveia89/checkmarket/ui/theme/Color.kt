package io.github.joaogouveia89.checkmarket.ui.theme

import androidx.compose.ui.graphics.Color

val CheckMarketRed = Color(0xFFE90305)
val CheckMarketRedLight = Color(0xFFFF5F52)  // Lighter shade of red
val CheckMarketRedDark = Color(0xFF9B0000)   // Darker shade of red

val CheckMarketBackground = Color(0xFFF2F2F2)  // Light gray background
val CheckMarketSurface = Color(0xFFFFFFFF)     // White surface
val CheckMarketOnPrimary = Color(0xFFFFFFFF)   // Text color on primary
val CheckMarketOnSecondary = Color(0xFF000000) // Text color on secondary

val CheckMarketPrimary = CheckMarketRed
val CheckMarketPrimaryVariant = CheckMarketRedDark
val CheckMarketSecondary = Color(0xFF03DAC5)  // Teal
val CheckMarketSecondaryVariant = Color(0xFF018786)
val CheckMarketOnBackground = Color(0xFF000000) // Text color on background
val CheckMarketOnSurface = Color(0xFF000000)    // Text color on surface

val CheckMarketErrorRed = Color(0xFFD32F2F)  // A deep, intense red for error
